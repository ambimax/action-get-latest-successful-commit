description: Gets the last successful commit on the current branch.

with:
  github_token: ${{ secrets.GITHUB_TOKEN }}
  workflow_id: ${{ github.workflow }}
  repository: ${{ github.repository }}
  branch: ${{ github.ref }}
